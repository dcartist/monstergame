import React from 'react';

function Results (props) {
    return (
        <div>
            Results
            {props.UserWarrior}
            {props.UserweaponSelect}
        </div>
    );
}

export default Results;